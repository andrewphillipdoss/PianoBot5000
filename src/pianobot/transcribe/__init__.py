"""Audio-to-MIDI transcription path (Demucs + Basic Pitch + Chordino +
allin1). Kept as an optional, self-contained mode -- the project's
primary path is now the chart-based one in ``pianobot.charts``, which
starts from a known chord/melody chart instead of trying to guess one
from a recording. See the top-level README's "Transcribe mode" section.
"""
