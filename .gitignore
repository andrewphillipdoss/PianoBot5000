# Everything that predates the recorder rebuild -- the old Python
# chart/transcribe CLI, its tests/examples, the web/Blender
# visualizers, and so on. Kept on disk for reference, not tracked.
/legacy/
